{
  "name": "rusclimbing-search",
  "version": "1.0.0",
  "description": "API для поиска соревнований по скалолазанию",
  "main": "app/api/main.py",
  "scripts": {
    "start": "uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo 'Build complete'",
    "dev": "uvicorn app.api.main:app --host 0.0.0.0 --port 8000 --reload",
    "lint:backend-ruff": "ruff check app",
    "lint:backend-pylint": "pylint --load-plugins=pylint_fastapi_plugin app",
    "lint": "npm run lint:backend-ruff && npm run lint:backend-pylint",
    "format": "prettier --write \"api/**/*.py\"",
    "format:check": "prettier --check \"api/**/*.py\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "vercel": {
    "buildCommand": "npm run build",
    "outputDirectory": ".",
    "installCommand": "npm install",
    "framework": "fastapi"
  }
}
